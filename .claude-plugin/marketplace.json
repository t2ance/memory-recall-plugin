{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "memory-recall",
  "description": "Forces Claude to check memory files before responding. Injects a reminder with available topic files on every user prompt.",
  "owner": {
    "name": "peijia"
  },
  "plugins": [
    {
      "name": "memory-recall",
      "description": "Per-turn memory recall reminder. Injects a CRITICAL reminder on every UserPromptSubmit, listing available topic files so Claude proactively reads relevant memories before responding.",
      "category": "productivity",
      "source": "./plugins/memory-recall"
    }
  ]
}
